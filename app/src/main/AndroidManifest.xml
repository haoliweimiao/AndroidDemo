<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hlw.demo">

    <application
        android:allowBackup="true"
        android:name="DemoApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.LoadingDialogShowActivity" />
        <activity android:name=".activity.ViewListActivity" />
        <activity android:name=".activity.LoadingViewsActivity" />
        <activity android:name=".activity.HorizontalDemoActivity" />
        <activity android:name=".activity.opengl.OpenglDemoActivity" />
        <activity android:name=".activity.opengl.OpenGLListActivity" />
        <activity android:name=".activity.opengl.OpenglAirHockeyActivity" />
    </application>

</manifest>